{
  "name": "@moge/types",
  "version": "1.0.0",
  "description": "Shared types for Next.js and NestJS",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --watch --project tsconfig.json",
    "type-check": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
