{
  "name": "@moge/configs",
  "version": "1.0.0",
  "description": "Shared configurations for monorepo",
  "main": "index.js",
  "scripts": {
    "build": "tsc && mkdir -p dist && cp eslint.config.mjs dist/"
  },
  "exports": {
    "./eslint.config": "./dist/eslint.config.mjs",
    "./tsconfig": "./dist/tsconfig.json"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "catalog:",
    "@typescript-eslint/parser": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-prettier": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  }
}
