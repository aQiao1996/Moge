/**
 * @deprecated This file is deprecated.
 * The manual `fetch` implementation has been replaced by the new tRPC React Query integration.
 * Please use the typed hooks from `~/lib/trpc` instead.
 *
 * @example
 * import { api } from '~/lib/trpc';
 * const { data } = api.auth.me.useQuery();
 */
